# GitHub 업로드 전 확인

> 이 폴더는 Git 제외 규칙을 적용해 소스만 복사한 새 저장소입니다. 초기화 시 커밋 0개·원격 연결 0개를 확인했습니다. 아래 과거 커밋 주의 사항은 원래 작업 폴더의 감사 기록이며, 해당 이력은 이 저장소에 복사하지 않았습니다. 실제 설정·업무 데이터·PHP 런타임도 포함하지 않았습니다.

점검일: 2026-09-15. GitHub 연결·커밋·푸시는 수행하지 않았다.

## 현재 작업 파일

- 실제 설정은 제외하고 php-version/config/example.php만 포함한다.
- DB·증빙·세션·로그·임시 폴더·비공개 전달 자료·백업·압축본·네이티브 설치 파일을 제외한다.
- CSV/Excel/PDF 및 SQL 내보내기도 제외한다. 소스 스키마인 php-version/database/*.sql은 포함한다.
- .env 예시와 테스트 코드는 공개용 예시/합성 데이터인지 검토한 뒤 커밋한다. 예시 파일명만으로 비밀값이 제거되지는 않는다.
- 과거 복구 자료의 실제 보관 경로는 Git 제외된 비공개 문서로 분리했다.
- 현재 후보 및 이미 스테이징된 내용에서 실제 업무 DB 사용자 이름/계정/ID/비밀번호 해시와 주요 비밀키 패턴을 대조했다. 점검 시 검출되지 않았다.
- 새 제외 규칙에 해당하면서 이미 추적 중인 파일은 없었다. 따라서 실제 파일 삭제나 index 제거는 하지 않았다.

## 과거 커밋 주의

과거 커밋의 삭제된 화면 소스 2개 버전에서 현재 업무 계정 식별자와 일치하는 문자열이 검출되었다. 실제 값은 이 문서에 기록하지 않는다. 과거 이력에서 점검한 비밀키/비밀번호 해시 패턴 및 DB·백업 파일 경로는 검출되지 않았다.

**.gitignore는 과거 커밋에 적용되지 않는다. 현재 .git 이력을 그대로 GitHub에 푸시하면 과거 계정 문자열도 포함된다.** 공개/비공개 저장소 여부와 무관하게 개인정보를 올리지 않으려면 업로드 전에 기존 이력을 정리하거나, Git 제외 규칙을 적용한 현재 소스만 별도 새 저장소로 전달해야 한다. 이번 작업에서는 기존 커밋을 재작성하지 않았다.

## 업로드 직전

```powershell
git status --short
git diff --cached --name-only
git diff --cached
```

git add -f로 제외를 우회하지 않는다. GitHub 웹의 파일 업로드/폴더 압축에는 .gitignore가 적용되지 않는다. 새 인증키나 직원 정보가 코드·문서에 직접 들어가면 경로 제외만으로 차단되지 않으므로 diff 내용을 확인한다.

점검은 로컬의 패턴·실제 업무 계정 대조 범위이며 모든 종류의 비밀정보 부재를 보증하지 않는다. 테스트 계정은 격리 검증용 합성 데이터다. 로컬 점검 상세 결과는 Git 제외된 .tmp/에 보관한다.
